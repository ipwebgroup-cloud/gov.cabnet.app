# Patch: v3.1.11 Shell Note + Observation Overview Navigation Fix

Upload `public_html/gov.cabnet.app/ops/_shell.php` to:

`/home/cabnet/public_html/gov.cabnet.app/ops/_shell.php`

No SQL is required.

This is a text/navigation-only patch. It keeps the V3 Observation Overview navigation and normalizes the shared shell side-note.
