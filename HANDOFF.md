# HANDOFF — v3.0.64 V3 Pre-Live Switchboard Ops 500 Hotfix

The V3 pre-live switchboard CLI worked, but the Ops route returned HTTP 500. This patch replaces only the Ops renderer with a defensive read-only page.

Live submit remains disabled. V0 remains untouched.
