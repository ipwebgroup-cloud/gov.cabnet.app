# gov.cabnet.app_config

This folder is designed to live outside `public_html` on cPanel.

Commit only `*.example` files. Keep real `config.php`, `bolt.php`, database passwords, Bolt credentials, internal API keys, EDXEIX session cookies, and CSRF tokens on the server only.
